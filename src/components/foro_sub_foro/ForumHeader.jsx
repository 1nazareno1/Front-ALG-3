// src/components/forum/ForumHeader.jsx
import { Box, Typography } from '@mui/material';

export const ForumHeader = () => (
  <Box sx={{ backgroundColor: '#BDBDBD', p: 4, textAlign: 'center' }}>
    <Typography variant="h6">Cabecera con imagen y/o texto</Typography>
  </Box>
);
